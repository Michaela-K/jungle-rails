<header class="page-header">
  <h1>Jungle Store Stats</h1> 
</header>

<table class="table table-hover border-success">
    <thead>
      <tr>
        <th> Products Count </th>
        <th> Categories Count</th>
      </tr>
    </thead>
    <tbody>
      <td> <%= @products %> </td>
      <td> <%= @category%> </td>
    </tbody>
</table>

<%= link_to 'All Products', [:admin, :products], class: 'btn btn-success' %>

